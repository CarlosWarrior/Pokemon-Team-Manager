export interface TypeModel {
    _id?: string,
    name: string;
    color: string;
    image: string;
    teracrystalImage: string;
}

export interface AbilityModel {
    _id?: string,
    name: string;
    description: string;
}